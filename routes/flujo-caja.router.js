const { Router } = require("express");

const {
    
} = require("../controller/flujo-caja.controller.js");
const router = Router();
/**
 * [API Documentation]
 * /api/flujo-caja
 */

// router.get("/", );
module.exports = router;
